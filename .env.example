VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_OAUTH_WEB_CLIENT_ID
# Optional for production builds outside GitHub Actions:
# VITE_BASE_PATH=/being-better/

# Adapter selection:
# VITE_DATA_BACKEND=google
# VITE_DATA_BACKEND=local_api

# Local API adapter base URL (used only when VITE_DATA_BACKEND=local_api)
# VITE_LOCAL_API_BASE_URL=http://localhost:8787

# Local Bun backend env:
# PORT=8787
# SQLITE_PATH=./data/being-better.sqlite
