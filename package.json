{
  "name": "being-better",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:backend": "bun backend/server.ts",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
